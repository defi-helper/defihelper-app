overwrite: true
schema: "https://backend-local.defihelper.io/api"
documents: [
  './src/**/**/*.graphql.ts',
]
config:
  scalars:
    AddressType: string
    DateTimeType: string
    MetricColumnType: string
    UuidType: string
generates:
  src/graphql/_generated-fragment-types.ts:
    plugins:
      - add:
          content: '/* eslint-disable */'
      - fragment-matcher
  src/graphql/_generated-types.ts:
    config:
      withHooks: true
    plugins:
      - add:
          content: '/* eslint-disable */'
      - typescript
      - typescript-operations
      - typescript-urql
hooks:
  afterAllFileWrite:
    - prettier --write
