import { style } from '@vanilla-extract/css'

export const root = style({
  width: 400,
  borderRadius: 12,
  padding: 40,
  position: 'relative',
})

export const title = style({
  marginBottom: 15,
})
